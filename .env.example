# CRITICAL SECURITY SETTINGS - SET THESE BEFORE PRODUCTION!

# Generate with: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-minimum-64-characters-long

# Admin credentials - CHANGE THESE!
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=your-super-secure-admin-password

# Google Workspace Email Configuration
# Option 1: SMTP with App Password (Recommended for Google Workspace)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-workspace-email@yourdomain.com
SMTP_PASS=your-app-password
SMTP_FROM=your-workspace-email@yourdomain.com

# Option 2: Gmail OAuth2 (Advanced)
# GMAIL_CLIENT_ID=your-oauth-client-id
# GMAIL_CLIENT_SECRET=your-oauth-client-secret
# GMAIL_REFRESH_TOKEN=your-refresh-token

# Frontend URL for links
FRONTEND_URL=https://your-frontend-url.com

# Database/Storage
NODE_ENV=production
PORT=5000

# WARNING: Never commit .env files to version control!